
<div class="blog-post">
  <h2 class="standart-h2title">Project: <%= @project.title %></h2>

  <p class="meta-blog clearfix" style='text-transform: capitalize' >
    <i class="icon-user"></i> Posted By: <%= @project.creator.name %> | 
    <i class="icon-envelope"></i> <%= link_to 'Contact' %> | 
    <i class="icon-list"></i> Category: <%= link_to "#{@category}" %> | 
    <i class="icon-user"></i> Project status: <%= @project.status %>
    <% if current_user && current_user.id == @project.creator.id %> 
     | <i class="icon-edit"></i> <%= link_to 'Edit this Project', edit_project_path(@project) %>
    <% end %>
  </p>
  <h2 class='big-text'>Description:</h2> <br> <h3 class='span8' style='margin:auto; float:none;'><%= @project.description %></h3>
</div>
